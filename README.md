# Personal Assistant CLI

CLI application for managing contacts and birthdays.

## Run locally
poetry run python -m src.main

## Docker
docker build -t personal-assistant .
docker run -it personal-assistant